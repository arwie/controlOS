#!/bin/sh

HOST=sys

SCRIPT=$0
BASEDIR=$(dirname $SCRIPT)

cd $BASEDIR

ssh -i ssh/id_rsa root@$HOST mount -o remount,rw /
scp -i ssh/id_rsa -r ../mc root@$HOST:/usr/lib/  &&  ssh -i ssh/id_rsa root@$HOST mc-update
