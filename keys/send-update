#!/bin/sh

HOST=sys

SCRIPT=$0
BASEDIR=$(dirname $SCRIPT)

cd $BASEDIR


scp -i ssh/id_rsa ../images/update root@$HOST:/mnt/init/.update \
	&& ssh -i ssh/id_rsa root@$HOST mv /mnt/init/.update /mnt/init/update
